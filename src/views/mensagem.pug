doctype html
html( lang='pt-br' )
	include head.pug
	body
		header
			h2 #{ titulo }
		main
			p( class="mensagem" ).
				#{ mensagem }
		footer
			a( href=`/sessao/${usuario}` ) voltar para painel
			a( href="/" ) encerrar sessao
			small lpfinancas 2023
